import { SearchByEmailPipe } from './search-by-email.pipe';

describe('SearchByEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
