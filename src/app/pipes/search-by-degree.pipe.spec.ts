import { SearchByDegreePipe } from './search-by-degree.pipe';

describe('SearchByDegreePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByDegreePipe();
    expect(pipe).toBeTruthy();
  });
});
